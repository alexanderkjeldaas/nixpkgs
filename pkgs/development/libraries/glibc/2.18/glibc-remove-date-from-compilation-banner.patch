diff -ur glibc-2.17.orig/csu/Makefile glibc-2.17/csu/Makefile
--- glibc-2.17.orig/csu/Makefile	2012-12-25 04:02:13.000000000 +0100
+++ glibc-2.17/csu/Makefile	2013-08-19 16:01:57.132378550 +0200
@@ -172,7 +172,7 @@
 		     os=Linux; \
 		   fi; \
 		   printf '"Compiled on a %s %s system on %s.\\n"\n' \
-			  "$$os" "$$version" "`date +%Y-%m-%d`";; \
+			  "$$os" "$$version";; \
 	   *) ;; \
 	 esac; \
 	 files="$(all-Banner-files)";				\
diff -ur glibc-2.17.orig/Makefile glibc-2.17/Makefile
--- glibc-2.17.orig/Makefile	2012-12-25 04:02:13.000000000 +0100
+++ glibc-2.17/Makefile	2013-08-16 23:07:41.881746274 +0200
@@ -142,7 +142,7 @@
 	 $(AR) x ../libc_pic.a; \
 	 rm $$($(AR) t ../sunrpc/librpc_compat_pic.a | sed 's/^compat-//'); \
 	 $(AR) x ../sunrpc/librpc_compat_pic.a; \
-	 $(AR) cr libc_pic.a *.os; \
+	 $(AR) crD libc_pic.a *.os; \
 	 rm *.os)
 endif
 
diff -ur glibc-2.17.orig/sunrpc/Makefile glibc-2.17/sunrpc/Makefile
--- glibc-2.17.orig/sunrpc/Makefile	2012-12-25 04:02:13.000000000 +0100
+++ glibc-2.17/sunrpc/Makefile	2013-08-16 23:11:03.710368143 +0200
@@ -122,7 +122,7 @@
 ifeq (yes,$(build-shared))
 subdir_lib: $(objpfx)librpc_compat_pic.a
 $(objpfx)librpc_compat_pic.a: $(rpc-compat-routines.os)
-	$(AR) cr$(verbose) $@ $^
+	$(AR) crD$(verbose) $@ $^
 $(rpc-compat-routines.os): $(objpfx)compat-%.os: %.c $(before-compile)
 	$(compile-command.c) -DEXPORT_RPC_SYMBOLS
 endif
